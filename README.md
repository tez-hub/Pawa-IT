This is a data analysis project built in FastAPI and React.

Run the backend
cd backend
pip install -r requirements.txt
uvicorn main:app --reload


Run the frontend
cd datasage
npm install
npm run dev

